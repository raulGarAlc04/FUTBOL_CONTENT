MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=futbol_data
PORT=4000

# Ruta al JSON de marcas visuales (por defecto: ./data/clasificacion_visual.json dentro de /api)
# MARKS_JSON_PATH=

# --- Login panel admin (cookie httpOnly + JWT) ---
# Mínimo 8 caracteres, aleatorio en producción
# JWT_SECRET=cambia_esto_por_un_secreto_largo
# AUTH_USER=admin
# AUTH_PASSWORD=tu_contraseña

# Opcional: acceso por cabecera (scripts, Postman) además de la sesión
# ADMIN_KEY=
